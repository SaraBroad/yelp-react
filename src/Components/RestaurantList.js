import React from "react";

const RestaurantList = ({}) => {
    // check if restaurant has pick-up or delivery
    return (
        <div className="ui relaxed divided list">Restaurant List</div>
    )
}

export default RestaurantList